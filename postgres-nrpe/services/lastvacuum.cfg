define service {
    service_description   PG-Last_Vacuum
    use                   postgres-nrpe-service-metric
    register              0
    host_name             postgres-nrpe
    check_command         check_nrpe!check_pg_lastvacuum
    service_dependencies  PG-Connection

    check_interval        10
}
