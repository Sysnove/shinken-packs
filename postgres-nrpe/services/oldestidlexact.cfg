define service {
    service_description   PG-OldestIdlexact
    use                   postgres-nrpe-service
    register              0
    host_name             postgres-nrpe
    check_command         check_nrpe!check_pg_oldestidlexact
    service_dependencies  ,PG-Connection

    business_impact 0
    notification_options 
    escalations

    _DETAILLEDESC         Check the oldest idle transaction.
    _IMPACT               Not an urgent problem.
    _FIXACTIONS           Use pg_activity to find which transaction is idle. Talk to the client.
}
