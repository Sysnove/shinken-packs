define service {
    service_description    fpm-status
    use                    generic-service
    register               0
    host_name              php-fpm
    check_command          check_fpm_status
}
