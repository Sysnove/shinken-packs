define service {
    service_description    fpm-status $KEY$
    use                    generic-service
    register               0
    host_name              php-fpm
    check_command          check_fpm_status!$KEY$
    aggregation php-fpm

    duplicate_foreach      _fpm_pools
}
