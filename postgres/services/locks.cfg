define service{
   service_description           PG-Locks for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgresql_locks!$KEY$

   duplicate_foreach _postgresql_databases
}
