define service{
   service_description           PG-Query_Time for $KEY$
   use            postgres-service
   register       0
   host_name	  postgres
   check_command  check_postgres_query_time!$KEY$

   duplicate_foreach _postgresql_databases
}
