define service{
   service_description           PG-table_size for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgresql_table_size!$KEY$

   duplicate_foreach _postgresql_databases
}
