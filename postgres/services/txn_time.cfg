define service{
   service_description           PG-Txn_time for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgres_txn_time!$KEY$

   duplicate_foreach _postgresql_databases
}
