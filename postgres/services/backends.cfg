define service{
   service_description           PG-backends for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgresql_backends!$KEY$

   duplicate_foreach _postgresql_databases
}
