define service{
   service_description           PG-connection for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgresql_connection!$KEY$

   duplicate_foreach _postgresql_databases
}
