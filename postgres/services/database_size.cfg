define service{
   service_description           PG-database_size for $KEY$
   use            generic-service
   register       0
   host_name	  postgres
   check_command  check_postgres_database_size!$KEY$

   duplicate_foreach _postgresql_databases
}
