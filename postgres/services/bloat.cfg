define service{
   service_description           PG-bloat for $KEY$
   use            postgres-service
   register       0
   host_name	  postgres
   check_command  check_postgres_bloat!$KEY$

   aggregation    postgresql/$KEY$

   duplicate_foreach _postgresql_databases
}
