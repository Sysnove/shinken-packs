define service {
    service_description LdapReplication
    use                 generic-service
    register            0
    host_name           ldap-consumer
    check_command       check_ldap_replication
}
