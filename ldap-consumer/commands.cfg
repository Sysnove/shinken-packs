define command {
    command_name check_ldap_replication
    command_line $PLUGINSDIR$/check_ldap_replication.py -p $_HOSTLDAPPROVIDER$ -c $HOSTADDRESS$ -b $_HOSTLDAPBASE$ -D $_HOSTDOMAINUSER$ -P "$_HOSTDOMAINPASSWORD$"
}
