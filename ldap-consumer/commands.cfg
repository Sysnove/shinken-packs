define command {
    command_name check_ldap_replication
    command_line $NAGIOSPLUGINSDIR$/check_ldap_replication -p $_HOSTLDAPPROVIDER$ -c $HOSTADDRESS$ -b $_HOSTLDAPBASE$ -D $_HOSTDOMAINUSER$ -P "$_HOSTDOMAINPASSWORD$"
}
