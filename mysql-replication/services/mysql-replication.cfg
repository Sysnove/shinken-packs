define service {
    service_description    mysql-replication
    use                    generic-service
    register               0
    host_name              mysql
    check_command          check_mysql_replication
}

