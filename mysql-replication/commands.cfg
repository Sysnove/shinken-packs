define command {
    command_name    check_mysql_replication
    command_line    $PLUGINSDIR$/check_mysql_slavestatus.sh -H $HOSTADDRESS$ -P 3306 -u $_HOSTMYSQLUSER$ -p $_HOSTMYSQLPASSWORD$ -w $_HOSTMYSQL_REPLICATION_LAG_WARN$ -c $_HOSTMYSQL_REPLICATION_LAG_CRIT$
}
