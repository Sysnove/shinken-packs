define host{
   name				auth-smtp
   use				generic-host
   register			0

   _SMTP_AUTHTYPE           LOGIN
   _SMTP_USERNAME			test@test.com
   _SMTP_PASSWORD			test
}

define host{
   name				auth-smtps
   use				generic-host
   register			0

   _SMTP_AUTHTYPE           LOGIN
   _SMTP_USERNAME			test@test.com
   _SMTP_PASSWORD			test
}
