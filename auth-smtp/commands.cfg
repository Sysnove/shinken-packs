define command {
    command_name     check_auth_smtp
    command_line     $PLUGINSDIR$/check_smtp -H $HOSTADDRESS$ -A $HOST_SMTP_AUTHTYPE$ -U $HOST_SMTP_USERNAME$ -P $HOST_SMTP_PASSWORD$
}

define command {
    command_name     check_auth_smtps
    command_line     $PLUGINSDIR$/check_smtp -H $HOSTADDRESS$ -A $HOST_SMTP_AUTHTYPE$ -U $HOST_SMTP_USERNAME$ -P $HOST_SMTP_PASSWORD$ -S
}
