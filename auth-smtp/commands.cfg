define command {
    command_name     check_auth_smtp
    command_line     $PLUGINSDIR$/check_smtp -H $HOSTADDRESS$ -A $_HOST_SMTP_AUTHTYPE$ -U $_HOST_SMTP_USERNAME$ -P $_HOST_SMTP_PASSWORD$
}

define command {
    command_name     check_auth_smtps
    command_line     $PLUGINSDIR$/check_smtp -H $HOSTADDRESS$ -A $_HOST_SMTP_AUTHTYPE$ -U $_HOST_SMTP_USERNAME$ -P $_HOST_SMTP_PASSWORD$ -S
}
