define service {
    service_description    auth-smtps
    use                    generic-service
    register               0
    host_name              auth-smtps
    check_command          check_auth_smtps

    aggregation            mail
}
