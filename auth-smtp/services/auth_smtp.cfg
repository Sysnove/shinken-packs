define service {
    service_description    auth-smtp
    use                    generic-service
    register               0
    host_name              auth-smtp
    check_command          check_auth_smtp

    aggregation            mail
}
