define service {
    service_description "Resque job queues"
    use nrpe-service,generic-service
    register 0
    host_name mapotempo-geocoder-server
    check_command check_nrpe!check_resque

    check_interval 60
    escalations
}
