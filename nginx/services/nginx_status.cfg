define service {
    service_description    nginx-status
    use                    generic-service
    register               0
    host_name              nginx
    check_command          check_nginx_status
}
